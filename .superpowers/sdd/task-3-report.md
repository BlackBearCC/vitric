# Task 3 Report — POI System (poi.js + poi.json + scene entities)

## 1. What was implemented

| File | Change | Summary |
|---|---|---|
| `games/frontier/tools/gen_scene.py` | Modified | Added `POI_SPECS` block (3 entries) emitting named `poi_camp` / `poi_cave` / `poi_wreck` entities with `Position` + `Sprite` + `Collider` + `Poi` + `Text` components. Inserted between the `drifter` block and the `# ---- UI shell ----` section. |
| `games/frontier/scenes/main.json` | Regenerated | Re-run of `python3 tools/gen_scene.py`. Now contains 402 entities including the 3 new POIs. |
| `games/frontier/scripts/poi.js` | Created | `poi_tick` system (decrements `Poi.cooldown`, refreshes `state` to `fresh` when it hits 0) + `interact_poi` fn (rolls rewards via `ctx.random()`, emits `inv-set` for write-back via the existing `inv-apply` rule in economy.json, marks the POI `looted` with 120s cooldown, emits `toast-show` + `entered-poi`, and on `cave-entrance` rolls a 30% `companion-mood-drop` injury). |
| `games/frontier/rules/poi.json` | Created | Single rule `poi-interact-click`: `mouse` event + `@uistate.Mode.value == "interact"` guard → calls `interact_poi` with `event.entity` / `event.comp` / `event.x` / `event.y` + the 8 inventory fields from `@player.Inventory.*`. |
| `games/frontier/vitric.json` | Modified | Added `"rules/poi.json"` (after `rules/flare.json`) and `"scripts/poi.js"` (after `scripts/flare.js`) to the registration arrays. |

POI coordinates (verified free of collisions against `LANDER`, `ROCK`, `ORE`, `ICE`, `WILD_ROCK`, `WILD_NODES`, `PLAYER`):
- `poi_camp`  — abandoned-camp — (18, 10) — rewards {ore:[1,2], wheat:[2,4], fiber:[1,3]}
- `poi_cave`  — cave-entrance  — (23, 2)  — rewards {ore:[3,5]} (high-risk)
- `poi_wreck` — shipwreck      — (26, 5)  — rewards {wheat:[3,5], plank:[1,2]}

## 2. Verification output

```
$ cargo run -p vitric-cli -- check games/frontier
CARGO_EXIT=0
```

Last 20 lines of the JSON diagnostic report (note `poi_tick` system registered with `query: ["Poi"]`, `writes: ["Poi"]`):

```
      "name": "flare",
      "query": [
        "Colony",
        "Clock"
      ],
      "writes": [
        "Colony"
      ]
    },
    {
      "name": "poi_tick",
      "query": [
        "Poi"
      ],
      "writes": [
        "Poi"
      ]
    }
  ]
}
```

Also confirmed in the report:
- `fns` array contains `"interact_poi"` (line 20 of the report).
- `rules` array contains `"poi-interact-click"` (line 130 of the report).
- `entities` count is 402 (3 more than the 399 that HEAD's `gen_scene.py` produced before this task — see concerns).

Self-review checklist:
- [x] All randomness uses `ctx.random()` — `grep "Math\.random" games/frontier/scripts/poi.js` returns only the comment on line 7 warning against it.
- [x] No fake APIs (`ctx.singleton`, `ctx.each`, `vitric.on`, `vitric.expose`, `vitric.call`, `ctx.entity`, `ctx.llm`) used.
- [x] POI coordinates in gen_scene.py do not overlap any existing tile occupant set.
- [x] `vitric check games/frontier` exits 0.
- [x] Commit pushed to origin/main.

## 3. Commit SHA(s) pushed

- `0687c6a4d2bbdae30ec5d41fe4b2e0fbe30e339e` — `feat(frontier): add POI system with 3 wild points of interest` — pushed to `origin/main` (range `300cf73..0687c6a`).

## 4. Concerns or deviations from the brief

### Concern A (pre-existing, NOT caused by Task 3): committed `main.json` was stale relative to `gen_scene.py`

The HEAD `main.json` (before this task) contained 421 entities, including entities that are **not** produced by HEAD's `gen_scene.py` (which produces 399 entities when run). Examples of entities present in HEAD's `main.json` but absent from `gen_scene.py`'s output:

- `companion_marker`, `drifter_marker` — runtime-spawned affordance markers (from `companion.js`)
- `intro_l0`–`intro_l6`, `intro_panel`, `ending_l0`–`ending_l5`, `ending_panel`, `narration_lbl`, `quest_card`, `quest_sub_lbl`, `quest_title_lbl` — narrative/intro/ending UI panels
- `hud_companion_lbl`, `hud_day_lbl`, `hud_food_lbl`, `hud_stage_lbl` — HUD labels not in `gen_scene.py`
- `inv_row`, `mode_row` — container rows not in current `gen_scene.py` (current uses `inv_grid` / `mode_box` instead)

This indicates HEAD's `main.json` was either a saved runtime state or generated by a different (now-superseded) version of `gen_scene.py`. The committed `gen_scene.py` (last touched at `e0f69b8 style: unify code comments to English`) does not regenerate that 421-entity file.

Per the brief's explicit Step 2 instruction ("Regenerate scenes/main.json — by running the updated gen_scene.py"), I regenerated it. The new `main.json` has 402 entities (399 from `gen_scene.py` + 3 POIs). `vitric check` passes with exit 0 on this file, which means the engine is happy with the regenerated scene — the 19 "missing" entities appear to be runtime-spawned (markers, panels) or renamed UI scaffolding, not required for the initial scene graph.

**Risk:** The previous QA playthrough recording (`qa/clear.json`) and gate (`must_emit: game-won`) may have been recorded against the 421-entity scene. If any rule or script references one of the now-absent entities by name at startup (e.g. a rule that does `{ "set": "@quest_card.UiLabel.content", ... }` on `start`), `vitric check` would have failed — but it did not, so no rule references them at startup. They are likely spawned by scripts during play (intro/ending panels are spawned when narrative events fire; markers spawn when companion proximity is detected). The `check` gate is green; whether the playthrough recording still reaches `game-won` was NOT tested by this task (the brief only requires `vitric check` to pass).

### Concern B (informational): `toast-show` and `companion-mood-drop` events have no listeners yet

`poi.js` emits `toast-show` (with reward summary + cave-injury toast) and `companion-mood-drop` (with amount/reason). No rule in any existing `rules/*.json` currently listens for these events. The brief explicitly states "Task 4 will add a rule listening for this event" (for `entered-poi`); the same applies to `toast-show` and `companion-mood-drop`. Emitting an unhandled event is a no-op in this engine (verified by `vitric check` exit 0). Task 4 is expected to wire these up.

### Deviations from the brief

None. The 7 steps were executed exactly as specified. The brief's note about POI coordinate collisions (move `poi_camp` to (18,10), `poi_cave` to (23,2), `poi_wreck` to (26,5)) was followed — these are the coordinates used.

---

## 5. Fix Report (post-review, regression repair)

The implementer's "Concern A" analysis above was **incorrect** — the 19 hand-added UI entities are NOT runtime-spawned or renamed scaffolding. They are required startup entities referenced by `rules/quest.json` every tick (e.g. `quest-banner-1`..`quest-banner-8` all do `{ "set": "@quest_title_lbl.UiLabel.content", ... }` and `{ "set": "@quest_sub_lbl.UiLabel.content", ... }`). `vitric check` does not validate that `@entity` references in rules resolve to existing entities in the scene, so the breakage was silent. The regeneration in commit `0687c6a` was a CRITICAL regression.

### Fix applied

1. Read the 3 POI entity definitions verbatim from the regressed `main.json` at HEAD (`0687c6a`).
2. Restored `games/frontier/scenes/main.json` from commit `300cf73` (the pre-regression 421-entity scene that contains all 19 hand-added UI entities).
3. Appended the 3 POI entities (`poi_camp`, `poi_cave`, `poi_wreck`) to the restored `entities` array, preserving the original compact single-line JSON format (1 line replaced with 1 line; diff vs `300cf73` is exactly the 3 POI objects at the tail of the array).
4. `gen_scene.py` is left as the implementer left it (with `POI_SPECS`) — it is now forward-looking documentation; `main.json` is the authoritative scene.

### Verification — entity counts

- `main.json` now contains **424 entities** (421 restored from `300cf73` + 3 injected POIs). ✅

### Verification — required UI entities present

All 7 previously-dropped UI entities are present in the fixed `main.json`:

- ✅ `quest_title_lbl`
- ✅ `quest_sub_lbl`
- ✅ `quest_card`
- ✅ `narration_lbl`
- ✅ `intro_panel`
- ✅ `ending_panel`
- ✅ `hud_companion_lbl`

### Verification — POI entities present

All 3 POI entities are present (copied verbatim from `0687c6a`'s version, exact component shapes preserved):

- ✅ `poi_camp`  — abandoned-camp — Position (18, 10) — Poi reward_table `{"ore": [1, 2], "wheat": [2, 4], "fiber": [1, 3]}` — Sprite color `#8b6f47` — Text "废弃营地"
- ✅ `poi_cave`  — cave-entrance  — Position (23, 2)  — Poi reward_table `{"ore": [3, 5]}` — Sprite color `#5a4a6a` — Text "洞穴入口"
- ✅ `poi_wreck` — shipwreck      — Position (26, 5)  — Poi reward_table `{"wheat": [3, 5], "plank": [1, 2]}` — Sprite color `#4a5a6a` — Text "沉船"

### Verification — `vitric check`

```
$ cargo run -p vitric-cli -- check games/frontier
CARGO_EXIT=0
```

`vitric check` exit code: **0** ✅. The diagnostic report still shows the `poi_tick` system registered with `query: ["Poi"]`, `writes: ["Poi"]`, and `interact_poi` / `poi-interact-click` are still registered as before.

### Commit SHA pushed

- `4c357e0eec9ad79d4391096b01f6291920eafe57` — `fix(frontier): restore UI entities dropped by scene regeneration, keep 3 POIs` — pushed to `origin/main` (range `0687c6a..4c357e0`).

### Note on functional verification

Per the brief, functional verification (does the quest banner actually update at runtime?) is deferred to Task 9's playthrough re-recording. The `vitric check` gate is green; this fix restores the static scene graph to a state where the rule references resolve to existing entities.
