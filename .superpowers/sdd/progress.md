# Frontier Sandbox Expansion — SDD Progress Ledger

Plan: docs/superpowers/plans/2026-07-20-frontier-sandbox-expansion.md
Base commit: c0c7af5
Branch: main (per user preference: auto-commit + push to main)

## Tasks

- Task 11: complete (commits f39c5f9 + post-review fix, APPROVED with 0 Critical/Important, 1 Minor resolved, 1 Nit)
  - Trading & Diplomacy: `Faction` component added to schema (4 fields: `relations` text JSON + `tier_nomads`/`tier_caravan`/`tier_remnant` text). `Colony._negotiate_target` text field added (LLM negotiation stash, mirrors `last_wish_memory_target` pattern). `Mode.value` enum extended with `"trade"` variant. `Faction` attached to `colony` entity (default relations `{"nomads":30,"caravan":0,"remnant":-10}`).
  - 3 factions: Nomads (荒原游民, wild, starting 30), Caravan (商队, desert, starting 0), Remnant (遗民, mountain, starting -10). Tier thresholds: hostile -100..-50, wary -49..10, neutral 11..40, friendly 41..75, allied 76..100. Rate mult by tier: hostile 0.5, wary 0.8, neutral 1.0, friendly 1.2, allied 1.5.
  - `faction.js` (NEW, 211 lines): constants (`FACTION_IDS`, `FACTION_INFO`, `RATE_MULT_BY_TIER`, `TRADE_OFFERS`, `NEGOTIATION_FALLBACKS`, `FACTION_LLM_ERROR_FALLBACK`); `tierFromRelation(v)` helper; `faction-tick` system (query [Colony, Faction], writes [Faction] — derives tier_<f> from relations JSON each tick); 5 fns (`change_relation`, `complete_trade`, `negotiate`, `onNegotiateReply`, `emit_reinforcement_hook`).
  - TRADE_OFFERS: nomads (3 wheat → 2 fiber), caravan (2 plank → 1 hide), remnant (2 hide → 1 crystal_core). Receive qty = `max(1, round(base * mult))`.
  - Relation drivers: +1 from trader companion (via `trade-available` event → `trader-companion-relation` rule → `change_relation("nomads", +1)`), +2 from `complete_trade`, +3 from `negotiate`. Clamped to [-100, 100].
  - LLM negotiation: `negotiate(faction)` → `ctx.ask("llm", prompt, "onNegotiateReply")` (mirrors `triggerWishMemory`/`onWishMemoryReply` pattern in wish.js). Stash target in `Colony._negotiate_target`. `onNegotiateReply` applies fixed +3 relation (deterministic — LLM only controls flavor text), detects `FACTION_LLM_ERROR_FALLBACK` text → substitutes faction-specific canned line from `NEGOTIATION_FALLBACKS` (3 lines per faction, deterministic pick by `cur % list.length`).
  - Forward-compat hooks: `relation-change` event (emitted by all 3 relation-modifying fns); `faction-allied-notify` rule (toast when relation transitions INTO 76+); `faction-reinforcements-available` event (emitted by `emit_reinforcement_hook` on `night-fall` if any faction is allied — forward-compat for Task 12/13 joint defense).
  - `faction.json` (NEW, 6 rules): `trader-companion-relation`, `negotiate-nomads/caravan/remnant`, `faction-allied-notify`, `faction-reinforcements-hook`.
  - `trade.json` (NEW, 3 rules): `trade-nomads/caravan/remnant` (ui-activate → `complete_trade` with full inventory passed).
  - `ui.json`: `mode-trade` (ui-activate action=mode-trade) + `kb-mode-trade` (B key); existing mode-build/craft/interact/research/combat + kb-mode-* rules extended to hide `@trade_menu`.
  - `hud.json`: `hud-faction-relation` rule — formats `游民[{}] 商队[{}] 遗民[{}]` from `@colony.Faction.tier_*` each tick.
  - Scene: `trade_menu` (top-left oy:176 w:280 h:360, initially hidden ox:-3000) + 3 trade buttons (trade_nomads/caravan/remnant, h:42 each) + 3 negotiate buttons (negotiate_nomads/caravan/remnant, h:36 each) + 6 stretch labels. `mode_trade` + `mode_trade_lbl` added to `mode_row` HBox (mode_row.Ui.w stays 582 — 6×92 + 5×6 = 582 exact fit). `relation_lbl` HUD (top-right oy:340 w:360 h:24, below hp_lbl oy:312+h:24=336, 4px gap).
  - Tests (`crates/vitric-cli/tests/faction.rs` NEW, 4 tests): `faction_tick_derives_tier_from_relations`, `change_relation_clamps_to_100`, `complete_trade_barters_items_and_adds_relation`, `on_negotiate_reply_applies_plus_3_relation`. All fast (≤2 ticks each).
  - **Schema field audit PASS**: `Faction.relations/tier_nomads/tier_caravan/tier_remnant`, `Colony._negotiate_target`, `Mode.value` ("trade" variant) all declared. Pre-existing fields re-used (Inventory.{ore,wood,fiber,seed,wheat,plank,chair,lamp,hide,crystal_core} all declared Task 8). No silent regressions.
  - **Tests**: faction 4/4 PASS, combat 4/4 PASS, research 4/4 PASS, seasons 4/4 PASS, companions 4/4 PASS, region 14/14 PASS (no regression), schema check exit 0, workspace all-green. Controller verified.
  - **Gate EXPECTED-FAIL**: `ReplayDiverged at tick 0` — adding Faction to colony + relation_lbl HUD + mode_trade button + trade_menu changes tick-0 world hash. Accepted per brief — `qa/clear.json` re-recorded in Task 15.
  - **Approved deviations**: (1) `set_relations` helper in test serializes JSON→string before `set_field` (Faction.relations is schema `text` field, not object — brief's literal `set_field(json!({...}))` would store object, break `JSON.parse` in faction-tick). (2) `complete_trade` test sets wheat/fiber AFTER tick 0 (seed-start rule at tick 0 would clobber fixture). (3) `on_negotiate_reply` test captures `llm-ask` event's `id` and injects it back in `llm-reply` (prelude's `__onReply` dispatcher requires `id` field with `#` separator to parse callback name prefix). (4) `faction.js` originally redeclared `const LLM_ERROR_FALLBACK` — QuickJS shared global would throw redeclaration error (wish.js already declares it). Initially removed and reused wish.js's declaration via shared global; **post-review fix** replaced with local `FACTION_LLM_ERROR_FALLBACK` const to make faction.js self-contained (no cross-file dependency).
  - **Post-review fix**: Reviewer M1 — `LLM_ERROR_FALLBACK` reused from wish.js via shared global was fragile (cross-file dependency on script load order). Renamed to `FACTION_LLM_ERROR_FALLBACK` declared locally in faction.js. No logic change, tests still pass.
  - Minor/Nit: N1 `faction-reinforcements-hook` emits on every night-fall when a faction is allied — no consumer yet (forward-compat for Task 12/13). Pure emit, deterministic. N2 schema.json lacks trailing newline (pre-existing, not introduced).
  - Cannot-verify items resolved by controller: faction/combat/research/seasons/companions/region tests PASS verified; schema check exit 0 verified; gate failure mode confirmed (`ReplayDiverged` at tick 0 — expected); diff stat 9 files +510/-19 verified matches report; vitric.json load order (faction.js at end) verified; `LLM_ERROR_FALLBACK` declaration in wish.js verified via Grep; `__onReply` id requirement verified via prelude.js read.

- Task 12: complete (commits 50776c0 + post-review fix, APPROVED with 1 Critical resolved, 1 accepted deviation, 2 Minor)
  - Map Expansion & Region Content: 5 regions (home/wild/mountain/swamp/desert) per spec §4.6. Home (0,0, 28×12) + wild (28,0, 32×30) active from start. Mountain (0,12, 30×28) dormant — unlock via exploration_t1 tech. Swamp (28,12, 32×28) dormant — unlock via explorer-role companion in party. Desert (60,0, 60×60) dormant — unlock via caravan relation ≥ neutral AND industry_t3 tech.
  - Engine: `ctx.thaw_region` JS bridge added (NEW — fixes pre-existing dead `unlock_region` fn from Task 8). Native `__thawRegion(id)` in lib.rs uses `with_sim_ptr(|sim| sim.thaw_region(id))` — mirrors `__randomStreamNext` pattern. `ctx.thaw_region(id)` in prelude.js. Before this, research.js's `unlock_region` fn called `ctx.thaw_region` which didn't exist → `TypeError` at runtime (pre-existing bug, no test covered it).
  - Engine: Camera `world_bounds` clamping in sim.rs `integrate_motion` (NOT runtime.rs — plan's file reference was wrong). At end of motion loop, before `follow_camera`. Clamps Camera.follow target's Position to `[min_x,min_y,max_x,max_y]` JSON. Zeros clamped axis Velocity. Only follow target clamped (other entities roam freely). Empty `world_bounds` = no clamping (backward-compatible).
  - Schema: `Camera.world_bounds` (text, default "") added.
  - `region.js` (NEW, 212 lines): `REGION_SPECS` (5 regions), `REGION_CONTENT` (per-region tile color + node specs + POI specs), `gen_region_content(region_id)` fn (uses `ctx.random_stream("region:<id>")` for deterministic tile/node/POI placement), `update_camera_bounds()` fn (queries all active region markers, computes union of rects, writes to Camera.world_bounds), `region-approach-check` system (every tick, checks player pos vs dormant region boundaries + unlock conditions → `ctx.thaw_region`), `checkUnlockCondition(id, ctx)` helper (mountain: has_exploration_t1; swamp: companion_handles has explorer; desert: tier_caravan ≥ neutral AND has_industry_t3).
  - `region.json` (NEW, 2 rules): `region-thaw-content` (on region-thaw → `gen_region_content`), `region-thaw-bounds` (on region-thaw → `update_camera_bounds`).
  - `world.js`: `genWild` extended from x16..27 y0..11 to x16..59 y0..29 (home transition x16..27 + expanded wild x28..59). 4 new resource nodes added in expanded area.
  - `research.json`: removed `research-unlock-desert` (incomplete condition — only checked tech, not caravan relation). Kept `research-unlock-mountain` (pure tech gate). Swamp + desert handled by `region-approach-check` system.
  - Scene: 4 region markers added (home/wild/swamp/desert). Mountain already existed. Camera entity gets `world_bounds: "[0,0,60,30]"` (home ∪ wild). Total entities 490→494.
  - Tests (`crates/vitric-cli/tests/region.rs` +152 lines, 3 new tests): `ctx_thaw_region_bridge_works_from_js` (calls unlock_region fn, verifies mountain thaws + mountain_node_0 spawns), `region_content_deterministic_across_thaw_timing` (thaw at tick 0 vs tick 1000 → identical node/POI positions), `camera_world_bounds_clamps_player_position` (player at x=19.9 with vx=+10, bounds [0,0,20,20] → clamped to x≤20, vx zeroed).
  - **Schema field audit PASS**: `Camera.world_bounds` (NEW) declared. All existing fields verified (Region.*, Camera.follow, Colony.Research.has_*, Colony.companion_handles, Colony.Faction.tier_caravan, Persona.role, Position.*, Velocity.*, Node.*, Poi.*).
  - **Tests**: region 17/17 PASS (3 new + 14 existing, no regression). Schema check exit 0. Controller verified.
  - **Gate EXPECTED-FAIL**: tick-0 world hash changes (new Camera.world_bounds field + 4 region marker entities). `qa/clear.json` re-recorded in Task 15.
  - **Approved deviations**: (1) `region-approach-check` system uses `query: ["Player"]` instead of brief's `query: []` — prelude rejects empty query arrays; "Player" is a valid schema tag component; system body ignores the batch and reads via `ctx.getField`. Acceptable.
  - **Post-review fix**: Reviewer C1 — `checkUnlockCondition` for swamp called `JSON.parse()` on `ctx.getField("colony", "Colony.companion_handles")`, but `companion_handles` is a `list` type field → `getField` returns a parsed JS array, not a JSON string. `JSON.parse(array)` throws → swamp permanently un-unlockable. Fix: removed `JSON.parse`, use array directly with `Array.isArray` guard + per-element type check. Mirrors `wish.js:21` pattern. Tests still pass.
  - Minor: M1 no test coverage for swamp/desert unlock paths (only mountain tested). M2 possible spawn position collisions when multiple regions' substreams produce overlapping coordinates (low risk — regions don't overlap geometrically).
  - Cannot-verify items resolved by controller: region 17/17 PASS verified; schema check exit 0 verified; 5 region markers + Camera.world_bounds in scene verified via Python; engine changes (__thawRegion bridge + camera clamping placement) verified via Read; diff stat 11 files +468/-14 verified; `companion_handles` list-type confirmed via wish.js:21 pattern (direct array use, no JSON.parse).

- Task 13: complete (commits 00e5789 + post-review fix, APPROVED with 0 Critical/Important, 1 Important non-blocking resolved)
  - Region Content Polish: Each region now has 2 POI types (mountain: ancient-ruins + crystal-cave; swamp: dangerous-flora + oasis; desert: caravan-stop + tomb). 6 POI handlers added to `poi.js` via `POI_HANDLERS` dict pattern (ancient-ruins: +3 TechPoint; crystal-cave: 30% mood-drop; dangerous-flora: 50% spawn gnawer; oasis: full party mood+5; caravan-stop: emit trade-available; tomb: 40% mood-drop curse). Handler dispatch added to `interact_poi` fn after standard reward roll — keeps legacy `cave-entrance` backward-compat path. `caravan` POI renamed to `caravan-stop`.
  - Sandbeast enemy (Task 12 forward-compat delivered): Added to `ENEMY_TYPES` table (damage 12, hp 60, aggro_range 12, drops hide[2,3] + crystal_core[1,2]). `desert-spawn` system added to `combat.js` (query [Region], writes [Region] — decrements desert marker's spawn_timer; when timer hits 0 AND player in desert bounds 60..119,0..59 → spawn sandbeast via `ctx.random_stream("desert_spawn")`; reset timer to 7200 ticks = 2 min real-time).
  - Post-review fix (controller-applied): Reviewer I1 — `oasis` handler emits `companion-mood-boost` event, but NO consumer existed anywhere in the codebase. The toast "全员心情+5" lied to the player. Root cause: brief §5 told implementer to check `rules/companion.json` for a mirror of `companion-mood-drop`, but the actual `companion-mood-drop` consumer lives in `rules/wish.json:86-88` (calls `apply_mood_drop` in `scripts/wish.js:147-159`). Fix: added `apply_mood_boost` fn in `wish.js` (mirrors `apply_mood_drop` — adds amount, capped at 100) + `companion-mood-boost-apply` rule in `wish.json`. Oasis toast now truthful.
  - Files modified (6 total): `scripts/region.js` (REGION_CONTENT expanded, +9/-1), `scripts/poi.js` (POI_HANDLERS dict + dispatch, +69/-2), `scripts/combat.js` (sandbeast + desert-spawn system, +56/-1), `scripts/wish.js` (apply_mood_boost fn, +14 post-review), `rules/wish.json` (companion-mood-boost-apply rule, +5 post-review). No new files.
  - Tests (`crates/vitric-cli/tests/region.rs` +2 tests, total 19): `sandbeast_spawns_when_player_in_desert` (force desert thaw + set player in desert bounds + advance 7200 ticks → assert Enemy entity exists), `sandbeast_does_not_spawn_when_player_outside_desert` (same setup but player in home bounds → assert no sandbeast). Both fast (≤2 ticks each).
  - **Schema field audit PASS**: No new schema fields (Task 13 is pure data + script logic). All fields read by new code already declared in Task 12 or earlier (Region.spawn_timer, Region.state, Position, Enemy.*, Need.comfort/comfort_i, Colony.companion_handles — all pre-existing). No silent regressions.
  - **Tests**: region 19/19 PASS (2 new + 17 existing, no regression). Controller verified.
  - **Gate EXPECTED-FAIL**: tick-0 world hash unchanged from Task 12 (no schema/scene changes); region content + sandbeast spawn only happen on desert thaw, which `qa/clear.json` doesn't trigger. Gate would still fail due to Task 12's accumulated changes. `qa/clear.json` re-recorded in Task 15.
  - **Approved deviations**: (1) `desert-spawn` system queries `["Region"]` instead of a more specific tag — Region is the only component on the desert marker, and the system filters by id === "desert" inside the body. Matches Task 12's `region-approach-check` pattern (query narrowest tag, filter in body). (2) Sandbeast spawn bounds `60..119, 0..59` hardcoded in `desert-spawn` system instead of reading `Region.anchor_x/w/h` — could be a `REGION_SPECS` lookup, but the desert marker's Region component already has the bounds and the system has the Region in its query. Accepted as a Minor (deterministic, easy to refactor). (3) POI handler dispatch runs AFTER standard reward roll — preserves backward-compat with the existing `cave-entrance` special-case path (kept for wild-area POIs). The `crystal-cave` handler duplicates the cave-injury effect but only fires for `kind === "crystal-cave"`, so no double-trigger.
  - Cannot-verify items resolved by controller: region 19/19 PASS verified; oasis mood-boost fix verified by reading `wish.js` + `wish.json` + tracing event flow (`oasis` handler emits `companion-mood-boost{amount:5}` → `companion-mood-boost-apply` rule calls `apply_mood_boost({amount:5})` → iterates `Colony.companion_handles` → `Need.comfort += 5` capped at 100, `Need.comfort_i = round(comfort)`).

- Task 10: complete (commits 318c7ec + post-review fix, APPROVED with 0 Critical/Important, 1 Minor resolved, 2 Nit)
  - Combat System: `Hp`/`Enemy`/`Weapon`/`Guard` components added to schema (Guard forward-compat — not attached). `Structure._cd_t` field added. `Mode.value` enum extended with `"combat"`. `Colony.enemy_snapshot` (JSON text, mirrors drifter-snapshot pattern) + `Colony.last_threat` (int, forward-compat) added.
  - Scene: player gets `Hp{100,100}` + `Weapon{stone_axe, dmg 10, range 2, cd 1}`. New `hp_lbl` HUD entity (top-right oy:312, below collective_wish_lbl oy:284+h:24=308, 4px gap). New `mode_combat` button + `mode_combat_lbl` label parented to `mode_row` HBox. `mode_row.Ui.w` bumped 580→582 (6×92 + 5×6).
  - `combat.js` (NEW, 376 lines): 4 fns (`spawn_wave`, `player_attack` [SECOND brief version — cooldown via `ctx.getField("@player","Weapon._cd_t")`], `apply_loot`, `retreat_all_enemies`) + 8 systems (`enemy-snapshot`, `enemy-ai`, `enemy-attack-player`, `enemy-attack-structures` [writes: Hp+Structure], `player-combat-cooldown`, `turret-auto-attack`, `guard-auto-defense` [role=guard, affinity>=60, continuous DPS], `player-respawn-check`). Shared global `STRUCTURE_HP_BY_TIER = {1:50, 2:100, 3:200}` declared here, referenced by economy.js. `ENEMY_TYPES` table: gnawer (dmg 5, hp 20, drops hide) + raider (dmg 8, hp 35, drops hide+crystal_core — requires mountain discovered AND day>=5). Sandbeast deferred to Task 13.
  - `economy.js`: `build` fn adds `Hp{tierHp, tierHp}` + `Structure._cd_t:0` to spawned structures. Dependency comment block at top noting `STRUCTURE_HP_BY_TIER` is from combat.js.
  - Damage model: enemy→structure/player uses continuous DPS (`damage * ctx.dt`); player/turret→enemy uses discrete swings with `_cd_t` cooldown (decremented by systems every tick, checked by fns on click).
  - `combat.json` (NEW, 4 rules): `night-fall-spawn-wave`, `dawn-break-retreat`, `combat-click` (Mode=combat gate, calls player_attack with weapon stats + click coords), `enemy-killed-loot` (calls apply_loot with event.loot).
  - `ui.json`: `mode-combat` (ui-activate action=mode-combat, hides all 3 menus) + `kb-mode-combat` (input action=f pressed) rules.
  - `hud.json`: `hud-hp` rule — `format "HP {}/{}"` on `@hp_lbl.UiLabel.content` every tick.
  - `vitric.json`: scripts array — `combat.js` loads BEFORE `economy.js` (after colony.js) for shared global. rules array — `combat.json` added after `research.json`.
  - `assets/enemy.png`: copied from `rock.png` via shell `cp` (per brief: NOT text_to_image API).
  - Tests (`crates/vitric-cli/tests/combat.rs` NEW, 4 tests): `enemy_spawns_on_night_fall`, `enemy_ai_moves_toward_player`, `player_attack_kills_enemy`, `player_respawns_on_death`. All fast (≤8 ticks each).
  - **Schema field audit PASS**: all 16 new fields declared (Hp.value/max, Enemy.kind/damage/aggro_range/home_region/_attack_cd, Weapon.kind/damage/range/cooldown/_cd_t, Guard.post_x/post_y/patrol_r, Structure._cd_t, Mode.value combat variant, Colony.enemy_snapshot/last_threat). Pre-existing fields re-used (Colony.player_x/y/food, Clock.day, Region.discovered, Position, Velocity, Inventory.*, Persona.role, Need.affinity) all still declared.
  - **Tests**: combat 4/4 PASS, research 4/4 PASS, seasons 4/4 PASS, companions 4/4 PASS, region 14/14 PASS (no regression), schema check exit 0, workspace all-green (skip typescript — pre-existing esbuild env issue). Controller verified.
  - **Gate EXPECTED-FAIL**: `ReplayDiverged at tick 0` — adding Hp+Weapon to player + hp_lbl HUD + mode_combat button changes tick-0 world hash. Accepted per brief — `qa/clear.json` re-recorded in Task 15.
  - **Approved deviations**: (1) `guard-auto-defense` uses continuous DPS (`GUARD_DAMAGE * ctx.dt`) instead of discrete swings — explicitly authorized by brief's inline design note (avoids adding `_combat_cd` to `Need`). (2) `event.loot` arrives at `apply_loot` fn as either string or object depending on engine serialization — fn defensively handles both. (3) `Colony.last_threat` + `Guard` component declared but unused (forward-compat hooks, like Task 9's `Colony.collective_wish_done` pattern).
  - **Post-review fix**: Reviewer M1 — `PLAYER_ATTACK_RANGE = 2` constant declared but never used (player_attack fn uses `a.weapon_range || 2` from rule-passed arg). Deleted the dead constant. No logic change, tests still pass.
  - Minor/Nit: N1 `mode_row.w=582` sized for 6 buttons but only 5 exist (no `mode_upgrade` button — upgrade mode entered via `upgrade-prompt` ui-activate, ~98 px slack). Faithful to brief's literal 582 spec. N2 `cache-player-pos` (companion.js) runs after `enemy-ai`/`enemy-attack-player` (combat.js), so they read 1-tick-stale player pos. Consistent with existing snapshot pattern; tests pass.
  - Cannot-verify items resolved by controller: combat/research/seasons/companions/region tests PASS verified; schema check exit 0 verified; gate failure mode confirmed (`ReplayDiverged` at tick 0 — expected); workspace all-green verified (skip typescript); diff stat 10 files +773/-4 verified matches report; vitric.json load order (combat.js before economy.js) verified.

- Task 9: complete (commits f3a6ef5 + 46ec1c6, APPROVED with 0 Critical/Important, 2 Minor)
  - Companions Expansion: `Persona.role` enum added (6 variants: builder/farmer/explorer/guard/trader/scholar) + `Colony.collective_wish_done` int flag. Scene: Pip (companion) role="builder", Lio (drifter) role="farmer", `collective_wish_lbl` HUD entity (top-right oy:284, anchor/parent/ox/w/h pattern matching techpoint_lbl/research_status_lbl siblings).
  - `companion.js`: DRIFTER_POOL 6→12 entries (2 per role × 6 roles). `companion-contribution` system query extended to include `Persona`, random pick REPLACED with role-based 6-way switch (builder→+1 plank, farmer→+0.5 food_rate, explorer→+1 fiber + emit explore-bonus, guard→+1 ore + emit guard-patrol, trader→+1 wheat + emit trade-available, scholar→emit tp-set). `default` fallback preserves old random behavior (defensive). `consumeDrifter` passes role through; `inviteAnyNearby` reads drifter Persona.role via ctx.getField and includes in `companion-invited` event payload. `WISH_TEMPLATES` 3→6 roles (guard/trader/scholar added); `wishesForRole(role)` direct lookup; `wishesForArchetype` kept as backwards-compat wrapper (archetype keyword → role mapping).
  - `wish.js`: `collective-wish-check` system added (query [Colony], writes []). One-time granary-50 milestone: when Colony.food_i >= 50 AND collective_wish_done == 0 → mark done, +10 affinity to all companion_handles, emit collective-wish-fulfilled + toast-show. NO duplicate WISH_TEMPLATES (see deviation #1).
  - `companion.json`: `drifter-cadence` rule split into `drifter-cadence-normal` (cap 8, 2-day cadence, stage != "兴旺") + `drifter-cadence-fast` (cap 8, 1-day cadence, stage == "兴旺") — mutually exclusive via if clauses. `companion-invite-process` rule passes `role: event.role`.
  - `hud.json`: `hud-collective-wish-pending` (if collective_wish_done == 0 → "共识: 粮储 {food_i}/50 (未达成)") + `hud-collective-wish-done` (if collective_wish_done == 1 → "✓共识达成: 粮储 50"). Two rules with if clauses (rule engine doesn't support if-then-else in `to` values).
  - Tests (`crates/vitric-cli/tests/companions.rs` NEW, 4 tests): `companion_contribution_role_builder_grants_plank`, `companion_contribution_role_scholar_grants_techpoint`, `collective_wish_fires_at_food_50`, `collective_wish_one_time_only`. All fast (1-2 ticks each).
  - **Schema field audit PASS**: `Persona.role` (enum, 6 variants), `Colony.collective_wish_done` (int) declared. All rule-read fields verified declared (Colony.stage, Colony.food_i, Colony.companion_handles, Inventory.plank/ore/fiber/wheat, TechPoint.value, Need.affinity/affinity_i — all pre-existing). No silent regressions (Task 8 lesson applied).
  - **Tests**: companions 4/4 PASS, research 4/4 PASS, seasons 4/4 PASS, region 14/14 PASS (no regression), schema check exit 0, workspace all-green. Controller verified.
  - **Gate EXPECTED-FAIL**: `ReplayDiverged at tick 0` — collective_wish_done field on Colony + collective_wish_lbl HUD entity change tick-0 world hash. Accepted per brief — `qa/clear.json` re-recorded in Task 15.
  - **Approved deviations**: (1) **`WISH_TEMPLATES` NOT duplicated in `wish.js`** — brief claimed QuickJS gives each script file its own scope, but the engine actually loads all scripts via `ctx.eval(source)` into a single shared global (`crates/vitric-script/src/lib.rs:525`). Duplicating `const WISH_TEMPLATES` would raise a redeclaration error. wish.js doesn't call those symbols (only companion.js's consumeDrifter does). Brief's "DRY violation" premise was a misreading. (2) Builder test does an extra "settle" step (seed-start rule sets Inventory.plank=6 on tick 0) — implementer steps once first, then primes companion + sets plank=0 before contribution step. Sound and deterministic. (3) `research_status_lbl` actual position is oy:256 (brief said oy:308) — collective_wish_lbl placement adjusted accordingly.
  - **Post-review fix (commit 46ec1c6)**: Reviewer I1 — collective_wish_lbl initially at top-left oy=336 overlapped with build_menu (top-left oy=176, h=700, visible at game start). Moved to top-right oy=284 (below research_status_lbl oy:256 + h:24 = 280, 4px gap), matching techpoint_lbl/research_status_lbl anchor pattern. No logic change, no test re-run needed.
  - Minor: M1 test setup inconsistency (builder test extra settle step vs scholar test skip — both pass, redundant given explicit plank=0 reset); M2 implementer's report cited wrong sibling group (same root cause as I1, fixed in post-review commit).
  - Cannot-verify items resolved by controller: companions/research/seasons/region tests PASS verified; schema check exit 0 verified; gate failure mode confirmed (`ReplayDiverged` at tick 0 — expected); QuickJS shared-global claim verified via Grep on `crates/vitric-script/src/lib.rs:525` (ctx.eval); WISH_TEMPLATES location verified via Grep (only in companion.js, comment in wish.js pointing to companion.js).

- Task 8: complete (commits b84e52b + c0d49a8, APPROVED with 0 Critical/Important, 4 Minor)
  - Tech Tree (12 nodes across 4 branches × 3 tiers): `Research` component (4 base fields known/current/progress/cost_total + 12 `has_<branch>_t<tier>` boolean mirrors) + `TechPoint` component added to schema. `Mode.value` enum extended with `"research"` only (NOT combat/trade — those are Tasks 10/11). `Inventory` extended with `hide` + `crystal_core`. Research attached to colony, TechPoint to player.
  - `research.js` (NEW): `TECH_TREE` constant (12 nodes — survival/agriculture/exploration/industry × 3 tiers, cost 2/4/8 TP, time 45/90/180s). `research-progress` system (query [Research], advances progress, on completion pushes id to known JSON array + sets has_* flag + emits researched event + toast). `tech-panel-hint` system (updates 12 tech_<id>_lbl UiLabel.content via ctx.setField to show ✓name / name NN% / name NTP). `start_research` fn (validates tech exists, not already known, prereq satisfied, TP sufficient; deducts TP via tp-set emit; sets Research.current/progress/cost_total). `unlock_region` fn (calls ctx.thaw_region — E1 API, safe no-op on missing region).
  - `economy.js`: BUILD table extended with `requires` field on tier-2/3 (plot2, monument) + 6 new recipes (well2, recycler, dome, hydroponics, arc_gun, turret — crystal_core as cost on tier-3). ITEMS extended with hide, crystal_core. `build` fn gains `requires` check (parses a.known, emits build-fail + toast if tech not met).
  - `poi.js`: `interact_poi` awards +2 TechPoints via tp-set emit. Inline ITEMS list extended.
  - Rules (NEW `research.json`): research-unlock-mountain (on researched{exploration_t1} → unlock_region mountain), research-unlock-desert (on researched{industry_t3} → unlock_region desert — no-op until Task 12), tp-apply (on tp-set → @player.TechPoint.value write-back).
  - Rules (MODIFY `economy.json`): build rule passes `known: @colony.Research.known`. Inventory-passing rules (craft-*, inv-apply, upgrade-click) extended with hide/crystal_core beyond brief — critical correctness fix preventing inv-set write-back from zeroing new fields.
  - Rules (MODIFY `poi.json`): interact_poi rule passes `techpoint: @player.TechPoint.value`.
  - Rules (MODIFY `ui.json`): mode-research + kb-mode-research (T key); existing mode-build/craft/interact/kb-mode-upgrade extended to hide tech_menu; 12 pick-tech-<id> rules calling start_research.
  - Rules (MODIFY `affordability.json`): build-dim-all extended with 6 new buttons; afford-plot2 + afford-monument gain tech `if` clauses; 6 new afford-<recipe> rules (well2, recycler, dome, hydroponics, arc_gun, turret) with cost + tech checks.
  - Rules (MODIFY `hud.json`): hud-techpoint + hud-research-status (format "研究中: {} ({}/{}s)").
  - Scene: techpoint_lbl (oy:286) + research_status_lbl (oy:314) HUD; tech_menu container (Grid 3 cols × 4 rows) + 12 tech_<id> buttons + 12 labels; mode_research button in mode_row (w bumped to 386); 6 new build_<recipe> buttons + labels in build_menu (h extended).
  - Tests (`crates/vitric-cli/tests/research.rs` NEW, 4 tests): research_progress_advances_with_dt, research_completes_after_time, start_research_deducts_techpoints, start_research_rejects_insufficient_techpoints. All fast.
  - **Schema field audit PASS**: all Research.*, TechPoint.value, Inventory.hide/crystal_core, Mode.value (research variant) declared. No silent regressions (Task 8 legacy lesson applied).
  - **Tests**: research 4/4 PASS, seasons 4/4 PASS, region 14/14 PASS (no regression), schema check exit 0, workspace all-green. Controller verified.
  - **Gate EXPECTED-FAIL**: `ReplayDiverged at tick 0` — Research/TechPoint on colony/player + new HUD entities change tick-0 world hash. Accepted per brief — `qa/clear.json` re-recorded in Task 15.
  - **Approved deviations**: (1) `Research.cost_total` typed `number` not `int` — enables brief's test shortcut (fractional values for fast completion); production unchanged (start_research sets it to integer tech.time). (2) `research-progress` reads cost_total from entity with `|| tech.time` fallback — enables test shortcut. (3) `vitric.json` manifest updated to register research.js + research.json (mechanical necessity, not in brief deliverables). (4) `kb-mode-craft`/`kb-mode-build`/`kb-mode-interact` not extended to hide tech_menu — pre-existing pattern (keyboard shortcuts only set Mode.value, don't sync menus); button-click rules do hide tech_menu correctly. (5) Inventory-passing rules in economy.json (craft-*, inv-apply, upgrade-click) and farm.json (interact-click) extended with hide/crystal_core beyond brief — critical correctness fix preventing inv-set write-back from zeroing new fields on every craft/interact/upgrade call.
  - Minor: M1 HUD label positions deviate from brief recommendation (techpoint_lbl at oy:280 not 286, research_status_lbl at oy:308 not 314) but non-overlapping; M2 pre-existing kb-mode-* keyboard shortcuts don't hide menus (out of scope); M3 idle research-status label shows raw "研究中:  (0/0s)" — pre-approved by brief; M4 rule `comment` field mixed Chinese/English — matches existing file convention.
  - Cannot-verify items resolved by controller: research/seasons/region tests PASS verified; schema check exit 0 verified; gate failure mode confirmed (`ReplayDiverged` at tick 0 — expected).

- Task 7: complete (commit f82d5ba, APPROVED with 0 Critical/Important, 2 Minor)
  - Seasons/Weather HUD — 7-day forecast bar: `Forecast` component (days text + last_day int) added to schema. Attached to `colony` entity. `forecast_lbl` HUD entity added to scene (oy:258, h:28, anchor/parent pattern matching season_lbl/weather_lbl).
  - `forecast-update` system in hud.js: query `["Clock","Season","Forecast"]`, writes `["Forecast"]`. Daily update gate (`if (e.Clock.day === e.Forecast.last_day) continue`). 7-label weighted-pick loop via `ctx.random_stream("forecast")` — isolated substream from flare.js's `"weather"` stream (7 draws/day don't perturb weather trajectory). `FORECAST_SEASONAL_WEIGHTS` duplicated from flare.js (brief-accepted DRY violation — extracting to shared module out of scope). `FORECAST_LABELS` maps weather keys to 晴/阴/雨/暴风/耀斑.
  - `hud-forecast` rule in hud.json: direct field copy `"to": "@colony.Forecast.days"` → `@forecast_lbl.UiLabel.content`. Uses the `{format, args}` rule engine API (NOT the brief pseudocode's `{call, with}` which was fictional — same correction as Task 6).
  - **Schema field audit PASS**: `Forecast.days`, `Forecast.last_day` declared; `Clock.day`, `Season.current` already declared in Task 6. No silent schema regressions (Task 8 lesson applied).
  - **UI layout PASS**: forecast_lbl at oy:258 has clean 6px gap above weather_lbl (oy:224 + h:24 = 248, then 10px gap to 258). No overlap.
  - **Tests**: seasons 4/4 PASS, region 14/14 PASS (no regression), schema check exit 0. Controller verified.
  - **Gate EXPECTED-FAIL**: `ReplayDiverged at tick 0` — Forecast on colony changes tick-0 world hash. Accepted per brief — `qa/clear.json` re-recorded in Task 15.
  - **Approved deviations**: (1) forecast_lbl Ui shape `anchor/parent/oy/w/h` (matches season_lbl/weather_lbl pattern) instead of brief's `x/y/ox/mode`. (2) `h:28` instead of brief's `h:0` (cosmetic). (3) Added `align:"center"` to match siblings.
  - Minor: M1 `FORECAST_SEASONAL_WEIGHTS` duplication from flare.js (brief-accepted); M2 forecast_lbl Ui shape deviation (approved per existing pattern).
  - Cannot-verify items resolved by controller: seasons/region tests PASS verified; schema check exit 0 verified; gate failure mode confirmed (`ReplayDiverged` at tick 0 — expected).

- Task 6: complete (commits 0b6adb9..e24d28f, APPROVED with 0 Critical, 1 Important resolved, 4 Minor)
  - Seasons & Weather: `Season` component (spring/summer/autumn/winter, 12 days each, 48-day year) + `Weather` component (clear/cloudy/rain/storm/flare) added to schema. Both components attached to `colony` entity (NOT a separate `clock` entity — the plan's pseudocode was fictional). `season_lbl` + `weather_lbl` HUD entities added to scene.
  - `clock.js` extended: query `["Clock","Season"]`, writes `["Clock","Season"]`. Season advance inlines inside `if (dayJustWrapped)` — `day_in_season += 1`, rolls over at 12 days, `season-change` event on rollover, `year += 1` on winter→spring wrap.
  - `flare.js` refactored into `weather-tick` system: query `["Colony","Clock","Season","Weather"]`, writes `["Colony","Weather"]`. Preserves `night-fall`/`dawn-break`/`flare-hit`/`flare-imminent` events. `flare-hit` fires on transition INTO flare (40% power/oxygen cut, once per onset). Weather weighted picking uses `ctx.random_stream("weather")` (E3 pattern — isolates weather RNG from main stream). Seasonal weights: flare only in summer (25%).
  - `crops.js`: `SEASON_CROP_MULT` (spring 1.2, summer 1.0, autumn 1.5, winter 0.3) applied to `ctx.dt` in main fn AND to `dormantSec` in catch_up fn. Season fetched via `ctx.getField("colony","Season.current")`.
  - `colony.js` tally: `WEATHER_RATE_MULT` (clear 1.0, cloudy 0.7, rain 0.7, storm 0.3, flare 0.0 for power; rain 1.5 for water) + `SEASON_RESOURCE_MULT` (spring 1.0, summer 0.8, autumn 1.2, winter 0.5) applied to emitted rates BEFORE `ctx.emit("tally",...)`. `apply-rates` rule unchanged.
  - Rules: `toast-weather-change` added to `flare.json`. `hud-season` + `hud-weather` added to `hud.json`.
  - Tests: `crates/vitric-cli/tests/seasons.rs` (NEW, 4 tests): `season_advances_on_day_boundary`, `season_rolls_over_at_12_days`, `year_increments_on_spring_wrap`, `weather_timer_decrements_each_tick`. All fast (1 tick each, boundary-condition setup).
  - **Gate EXPECTED-FAIL**: `ReplayDiverged` at tick 0 (adding Season/Weather to colony changes tick-0 world hash; weather RNG changes trajectory). Accepted — `qa/clear.json` re-recorded in Task 15. Schema check PASS, workspace tests PASS (skip typescript), seasons 4/4 PASS, region 14/14 PASS (no regression).
  - **Important (resolved by controller)**: brief pseudocode said `o2: extractor * PER * smult` but prose said "o2 gets only smult". Original `o2` source was `plot * PER` (plots produce both food AND oxygen — confirmed via `git show 0b6adb9:games/frontier/scripts/colony.js`). Brief pseudocode had a bug (changing source to extractor). Implementer correctly followed prose intent: `o2: plot * PER * smult` (kept original source, applied only season multiplier). Approved deviation.
  - **Approved deviations**: (1) Rule format `{format,args}` instead of brief's `{call:"toast",...}`/`{set,to}` — matches existing rules in `flare.json`/`hud.json` (brief pseudocode was wrong about rule engine API). (2) HUD positions moved from oy:150/184 to oy:190/224 to avoid overlap with `narration_lbl` (oy:150, h:40). (3) `Weather.next` declared but unused (forward-compat for Task 7 forecast — brief allows). (4) `flare-imminent` semantic inverted (now warns before flare ENDS, not STARTS — brief accepts).
  - Minor: M1 `total` field brief inaccuracy (cosmetic); M2 `flare-imminent` toast text may be misleading for Task 7 (cosmetic, fix in Task 7); M3 stale `progress.md` in working tree (correct — not staged per brief's `git add` list); M4 `Weather.next` unused (forward-compat).
  - Cannot-verify items resolved by controller: gate failure mode confirmed (`ReplayDiverged` at tick 0 — expected); workspace test pass trusted from implementer report (diff is consistent); schema check PASS verified by controller; region test no-regression verified by controller.

- Task 5: complete (commits 1537603..98fd213, APPROVED with 0 Critical/Important, 2 Minor)
  - E5 Snapshot/replay/describe plumbing: `describe_world_with_assets` gains a `dormant` classification pass — iterates `world.entities()` (bypassing `world.query`'s dormant filter), includes only entities with Position+Sprite that are `world.is_dormant(id)`. Each entry carries `id`, `name?`, `world` {x,y}, `sprite` {w,h,color,image?,rot?}, `region` {id, state}. Sort applied (named first, then id) when focal point exists. `"dormant"` key placed after `"offscreen"` in JSON output.
  - Snapshot preserves dormant state: locked by `snapshot_preserves_dormant_state` test (Region.state frozen + discovered=1 round-trips through `Sim::snapshot` + `Sim::restore`). Already works — Region is a World component.
  - Replay with dormant region hash-identical: locked by `replay_with_dormant_region_is_hash_identical` (60-tick recording with dormant mountain, replay matches final hash). Pre-recording thaw contract: locked by `replay_after_pre_recording_thaw_is_hash_identical` (thaw_region before both recording and replay — host-API-re-run contract per sim.rs line 362-365).
  - **Architectural note**: `thaw_region` DURING recording is NOT replayable via `Sim::replay` (by design — host API calls not recorded; `replay_observed` has no between-tick host hook). Replaced plan's impossible `replay_with_region_thaw_is_hash_identical` test with two correct tests. Adding a between-tick host hook to replay is out of scope for E5.
  - Gate hash unchanged: `0xab58ec29d99275df` (describe_world is read-only, snapshot/replay tests don't change production code path). Controller re-ran gate to verify.
  - Minor: M1 sort-closure reuse (improvement over brief pseudocode — DRY); M2 cosmetic test-comment nit. `mut rt` → `rt` in snapshot test (approved deviation — `sim.snapshot(&rt)` takes `&Runtime`, `rt` never used mutably).
  - Cannot-verify items resolved by controller: commit already pushed (98fd213, implementer's report was stale about cancellation); gate hash verified by controller rerun (pass:true); read-only signatures verified by reviewer via Grep.
  - **E1-E5 engine capability set COMPLETE.** Phase 1 done.

- Task 4: complete (commits c286ca5..58ab058, APPROVED with 0 Critical/Important, 3 Minor)
  - E4 View-frustum culling: `render_with` computes world-space viewport bounds from `(cam_x, cam_y, scale)` + framebuffer dimensions, then per-entity AABB check (rotation-aware `ext_x`/`ext_y` for `rot != 0`) before the pixel loop. Skipped entities are exactly those whose rotated AABB is entirely outside the viewport. Culling uses the shaken camera (same as the picture) so shake-panned entities are never lost.
  - **Dropped `margin = 4.0`**: brief pseudocode used margin "for shadow casters" but `collect_occluders` queries `["Solid","Position","Collider"]` via a separate path — sprite cull doesn't affect shadows. Exact AABB is the right boundary; any margin would be arbitrary over-conservatism. Approved deviation.
  - **Replaced flaky perf test with 3 deterministic correctness tests** (brief explicitly allows): `offscreen_entities_not_rendered` (regression guard — passes even without culling due to existing clamp, but locks contract), `onscreen_entities_rendered` (would fail if culling math has a sign flip), `culling_preserves_byte_identical_output_for_onscreen_entities` (asserts `Vec<u8>` equality + 32×32 magenta footprint). Screenshot-hash tests (frames/glow/particles) + gate hash lock the determinism contract end-to-end.
  - **GPU mirror skipped** (brief Step 5 optional): documented in `gpu.rs:1887-1894` pointing to CPU logic. CPU path is source of truth for screenshots/gate.
  - **`describe_world` not culled** (intentional): already classifies visible/offscreen with its own boundary check; off-screen entities appear in `offscreen` array for agent navigation. Culling would lose semantic info.
  - **Lights/Emitters not culled** (intentional): off-screen Light can illuminate on-screen pixels (radius extends into viewport); lighting formula already does per-pixel radius culling. Emitters draw via separate `draw_particles` path.
  - Gate hash unchanged: `0xab58ec29d99275df` (controller re-ran gate to verify — pass:true, verified:true, 37249 ticks).
  - Minor: M1 test header undercount ("two" → "three", fixed in post-review commit); M2 `offscreen_entities_not_rendered` is a weak TDD driver (useful regression guard, implementer honest); M3 test name `culling_preserves_byte_identical_output_for_onscreen_entities` slightly misleading (actual byte-lock comes from existing screenshot-hash tests + gate hash).
  - Cannot-verify items resolved by controller: typescript failures pre-existing (esbuild missing, verified via git stash in Task 1); gate hash verified by controller rerun (pass:true, hash matches); leftover uncommitted files committed in post-review docs commit.

- Task 3: complete (commits 53da162..c286ca5, APPROVED with 0 Critical/Important, 3 Minor)
  - E3 Seeded RNG substreams: `Substream` struct in `pcg.rs` (FNV-like hash of `(world_seed, name)` → increment); `Sim::substreams: HashMap<String, Substream>`; `Sim::random_stream(name) -> &mut Substream` inserts-if-absent; native `__randomStreamNext(name)` via `SIM_PTR` thread-local (mirrors `WORLD_PTR`); `ctx.random_stream(name)` in prelude returns `{ next(), nextInt(min, max) }`; substream state in `Sim::snapshot`/`restore` (serde_json BTreeMap-backed → byte-stable).
  - Substream state NOT in recording checkpoint hash (by design — recording hashes World only); determinism via `(world_seed, name)` seed contract + call-order replay.
  - Gate hash unchanged: `0xab58ec29d99275df` (substreams are Sim state, not World state).
  - Minor: M1 per-call FFI cost (revisit in Task 12); M2 test helper RAII (harmless); M3 safety comment imprecise (mirrors existing pattern).
  - Cannot-verify (Task 12 follow-ups): thaw_region not recorded by Recording — verify Task 12 region-thaw triggers are deterministic-given-recording; substream divergence only detected via world-state hash — verify Task 12 substream consumers write results into entity components.

- Task 2: complete (commits 202504a..53da162, APPROVED with 0 findings)
  - E2 Catch_up system API: `vitric.system(name, decl, fn, catch_up_fn?)` accepts optional 4th arg; `__runCatchUp` global in prelude; `SystemDecl.has_catch_up: bool`; `Sim::pending_catch_ups: Vec<String>` queue flushed in `step()` before `on_tick`; `GameLogic::catch_up_region` trait method (default no-op); `Runtime::catch_up_region` bridges to `ScriptEngine::run_catch_up_for_region`; `crop-grow` declares simplified catch_up (timer + stage only, no emit/side effects).
  - **Deviation approved**: queueing condition changed from `was_discovered` (brief pseudocode) to `was_dormant` — brief's `was_discovered` would skip catch_up on first thaw of never-discovered region, but test explicitly verifies catch_up runs on first thaw when entities exist in dormant region. `was_dormant` is semantically correct: dormant regions have un-simulated entities needing reconciliation; active regions have dormant_ticks=0 so catch_up is no-op anyway.
  - Cannot-verify items resolved by controller: region tests 5/5 PASS (catch_up test takes ~65s in debug due to 3600-tick step); gate PASS with same hash `0xab58ec29d99275df` (Task 2 didn't perturb deterministic trajectory — catch_up only fires on thaw, which existing recording doesn't do).
  - Pre-existing typescript failures (esbuild missing) unchanged.

- Task 1: complete (commits c0c7af5..a9e1167, APPROVED after I1 fix)
  - E1 Region dormant/active/frozen: `world.query`/`render_world`/`describe_world` skip dormant; `Sim::thaw_region(id)` transitions state + emits `region-thaw`; `accumulate_dormant_ticks` runs each step; `invoke_catch_up_for_region` stub added for Task 2 integration.
  - `Region` component added to `games/frontier/schema.json` (10 fields, state enum dormant/active/frozen). Mountain marker entity added to `scenes/main.json` (dormant, anchor 0,12, 30×28).
  - `pending_events: Vec<Event>` field added to `Sim` for host-side event emission (flushed at start of `step()`, not recorded by `Recording` — same determinism model as `pending_inputs`/`pending_replies`).
  - `qa/clear.json` re-recorded (37249 ticks, final_hash `0xab58ec29d99275df`, settlement-founded emitted) — necessary because mountain marker changed tick-0 hash.
  - Review I1 fix (commit a9e1167): added `was_discovered` conditional + `invoke_catch_up_for_region` stub.
  - Minor findings (non-blocking): M1 silent no-op on missing region (safer than brief's `.expect()`); M2 `thaw_region` not idempotent on already-active (rules can dedupe via `discovered`); M3 no positive-case test for `dormant_ticks` increment (brief didn't require).
  - Cannot-verify items resolved by controller: typescript failures pre-existing (esbuild missing); region tests 4/4 PASS verified; gate PASS with re-recorded qa/clear.json verified.

---

# Legacy: Frontier Deepening — SDD Progress Ledger

Plan: docs/superpowers/plans/2026-07-17-frontier-deepening.md
Base commit: af83c61
Branch: main (per user preference: auto-commit + push to main)

## Tasks

- Task 1: complete (commits af83c61..35004ad, review clean)
- Task 2: complete (commits 35004ad..300cf73, review clean after Math.random→ctx.random fix)
- Task 3: complete (commits 300cf73..4c357e0, APPROVED with 4 Minor, F2/F3 resolved in T4)
- Task 4: complete (commits 4c357e0..e01d4c0, APPROVED with 1 Minor)
- Task 5: complete (commits e01d4c0..eb0ac67, APPROVED with 3 Minor)
  - LLM memory dialogue: triggerWishMemory fn + onWishMemoryReply callback + MEMORY_FALLBACKS. Rule wish-fulfilled-memory catches wish-fulfilled → calls triggerWishMemory. Fallback path uses archetype-specific canned lines when LLM unavailable.
  - Minor findings: stash clobber on concurrent fulfillments (low probability), memory-unlocked event has no listener (intentional forward-looking), last_talk_reply shared with talk system (low conflict probability).
- Task 6: complete (commits eb0ac67..06464fb, APPROVED with 0 Critical/Important, 3 expected Minor)
  - Quest system converted to milestone: step3 gate on wish-fulfilled + affinity>=60; step6 gate on companion_wish_count>=2; game-won rule → settlement-founded (emits only settlement-founded, no settlement-thrived/game-won); quest-banner-8 → "自由探索中"; vitric.json gates.must_emit → settlement-founded.
  - Minor findings (all expected, non-blocking): dead ending-show rule in narrative.json left in place (YAGNI); qa/clear.json stale (Task 9 re-records); brief said check output "OK" but actual is JSON report with exit 0 (cosmetic).
- Task 7: complete (commits 06464fb..5a9870f, APPROVED with 0 findings)
  - Node regrowth: interact fn sets Node.cooldown=90 on depletion; node_regrow system decrements by ctx.dt, regrows left=max when cooldown hits 0.
  - Structure upgrade: upgrade_structure fn (plot→greenhouse, conduit→solar-array, quarters→cabin) + upgrade-button-click rule on ui-activate{action:"upgrade-prompt"}.
- Task 8: complete (commits 5a9870f..62c45fc, APPROVED with 1 cosmetic Minor)
  - UI hooks: flare-bar system (writes Colony.flare_bar) + hud-flare-bar rule; kb-mode-upgrade (key "u" → upgrade mode); upgrade-click rule (mouse + Mode=upgrade → upgrade_structure fn).
  - Minor: hud.js missing trailing newline (cosmetic, non-blocking).
  - **Post-review schema regressions fixed**: Task 8 added Colony.flare_bar writes + Mode.value="upgrade" sets + hud-flare-bar rule (reads @colony.Colony.flare_bar), but never added the schema field, the enum variant, or the @flare_lbl scene entity. Fixed in commit ee8cc74 (added Colony.flare_bar text field, "upgrade" to Mode.value enum, flare_lbl entity in main.json). Crashed engine at tick 0.
- Task 9: complete (commits 854c6ff, gate PASS — replay 37247 ticks verified, settlement-founded emitted)
  - Re-recorded 9-day playthrough with new wish-based quest gates.
  - Script bugs found & fixed in record_clear.py: (1) giveGiftNearby consumes ore (ore is first in ITEM_KINDS) → gather 4 ore not 2; (2) inp("e") only sets Mode.value, doesn't show craft_menu (only the mode-craft ui-activate rule shows it) → click mode_craft button before craft_plank.
  - Recording actually runs to day=11 (day-labels in script are aspirational, not literal); gate only checks settlement-founded emission.
  - **Pre-task schema regressions fixed**: Task 4 (commit e01d4c0) introduced Colony._wish_food_day and Colony.last_wish_memory_target writes in wish.js but never added them to schema.json. Fixed in commit 30e6ae7. Crashed engine at tick ~1191 (food reaches 80 after building any plot).
- Task 10: docs update — IN PROGRESS

## Notes

- Task 9 done via RPC-driven record_clear.py (no manual play). Controller rebuilt release binary, ran script, verified replay + gate.
- API: ctx.getField/setField/spawn/despawn/ask/emit/random/dt/tick all real. __onReply is prelude built-in. ctx.ask("llm", prompt, "callbackFnName") routes via llm-reply event → __onReply → callbackFn.
- Engine allows runtime ad-hoc fields on entities BUT rule/system reads via @entity.Comp.field require the field to be declared in schema.json. JS system writes (e.Colony.foo = ...) tolerate undeclared fields, but rule reads (@colony.Colony.foo) do NOT — they crash. Lesson: always declare fields in schema.json if any rule reads them.
- Task 6 changes: quest.json step3/6 gates → wish-based, step7→8 rename game-won→settlement-founded, quest-banner-8 text → "自由探索中", vitric.json gates.must_emit → settlement-founded. Colony.companion_wish_count already in schema (Task 1).
- Schema audit (Task 9): all Colony.* field references in scripts/*.js cross-checked against schema.json. All fields now declared.
