{
  "rules": [
    {
      "id": "win-on-key",
      "on": { "event": "input", "filter": { "action": "key", "phase": "pressed" } },
      "do": [
        { "emit": "game-won", "data": {} }
      ]
    }
  ]
}
